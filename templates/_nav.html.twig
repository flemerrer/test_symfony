<nav>
	<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
		<div class="container-fluid" style="justify-content: start; width:375px">
			<a class="nav-link" href="/"><img id="logo" src="{{ asset('img/logo.svg') }}" alt="Bucket" style=""></a>
			<h2 style=" margin:0; padding:.25rem 1rem; color: greenyellow">Bucket List</h2>
		</div>
		<div class="container-fluid">
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse"
					data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
					aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarSupportedContent" class="navbar-header">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li class="nav-item">
						<a class="nav-link" href="/">Home</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href={{ path('wish_list') }}>Bucket List</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href={{ path('wish_add') }}>Add wish</a>
					</li>
					{% if app.user %}
						<li class="nav-item">
							<a class="nav-link" href={{ path('app_logout') }}>Log Out</a>
						</li>
					{% else %}
						<li class="nav-item">
							<a class="nav-link" href={{ path('app_register') }}>Sign in</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href={{ path('app_login') }}>Log in</a>
						</li>
					{% endif %}
				</ul>
			</div>
		</div>
	</nav>
</nav>
