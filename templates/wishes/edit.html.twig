{% extends 'base.html.twig' %}
{% block title %}Edit | {{ parent() }}{% endblock %}
{% block body %}
	<div>
		<h2>Edit wish "{{ wish.title }}"</h2>

		{{ form_start(wishForm) }}

		{{ form_widget(wishForm) }}

		<button class="btn-primary btn-small">Save modifications</button>

		{{ form_end(wishForm) }}

	</div>
{% endblock %}
